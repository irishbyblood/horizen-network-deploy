# SSL Configuration Template
# This file provides SSL/TLS configuration once certificates are obtained
# Uncomment and configure after running ssl-setup.sh script

# SSL Certificate paths (Let's Encrypt default)
# ssl_certificate /etc/nginx/ssl/fullchain.pem;
# ssl_certificate_key /etc/nginx/ssl/privkey.pem;
# ssl_trusted_certificate /etc/nginx/ssl/chain.pem;

# SSL Protocols and Ciphers
# ssl_protocols TLSv1.2 TLSv1.3;
# ssl_prefer_server_ciphers on;
# ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';

# SSL Session settings
# ssl_session_timeout 1d;
# ssl_session_cache shared:SSL:50m;
# ssl_session_tickets off;

# OCSP Stapling
# ssl_stapling on;
# ssl_stapling_verify on;
# resolver 8.8.8.8 8.8.4.4 valid=300s;
# resolver_timeout 5s;

# HSTS (HTTP Strict Transport Security)
# add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;

# Additional security headers for HTTPS
# add_header X-Frame-Options "SAMEORIGIN" always;
# add_header X-Content-Type-Options "nosniff" always;
# add_header X-XSS-Protection "1; mode=block" always;

# Redirect HTTP to HTTPS
# server {
#     listen 80;
#     server_name horizen-network.com www.horizen-network.com druid.horizen-network.com geniess.horizen-network.com;
#     
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#     
#     location / {
#         return 301 https://$server_name$request_uri;
#     }
# }

# HTTPS Server block example
# server {
#     listen 443 ssl http2;
#     server_name horizen-network.com www.horizen-network.com;
#     
#     ssl_certificate /etc/nginx/ssl/horizen-network.com/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/horizen-network.com/privkey.pem;
#     
#     location / {
#         root /usr/share/nginx/html;
#         index index.html;
#         try_files $uri $uri/ /index.html;
#     }
# }

# HTTPS Druid subdomain example
# server {
#     listen 443 ssl http2;
#     server_name druid.horizen-network.com;
#     
#     ssl_certificate /etc/nginx/ssl/druid.horizen-network.com/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/druid.horizen-network.com/privkey.pem;
#     
#     location / {
#         proxy_pass http://druid_router;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
